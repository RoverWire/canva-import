# Rakefile
require 'dotenv/load'
require 'sinatra/activerecord'
require 'sinatra/activerecord/rake'
require 'random/formatter'
require_relative 'lib/canva'
Dir.glob('lib/tasks/*.rake').each { |r| load r}
